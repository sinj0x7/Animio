services:
  web:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: "3000"
      # Set in a `.env` file next to this compose file (see DEPLOY-ORACLE.md).
      CORS_ORIGINS: ${CORS_ORIGINS:-}
